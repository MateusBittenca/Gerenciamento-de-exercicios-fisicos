# Porta do servidor
PORT=3000

# Banco de dados MySQL
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=
DB_NAME=unifit

# CORS: use * em desenvolvimento ou a URL do frontend em produção (ex: http://localhost:5173)
CORS_ORIGIN=*
